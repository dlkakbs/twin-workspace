"""Internal Twin runtime namespace.

User-facing integrations should prefer `skills.twin` contract surfaces.
Modules under `skills.twin._runtime` are implementation details that may evolve
with the Twin package layout.
"""

